// This is helper code for generating the HTML that's going to be output to an .html file

// generateHTML function accepts a team input which is an array of objects

  // defaultHTML function will generate the default "Employee" HTML that'll handle the name, id, and email

  // engineerHTML function adds GitHub HTML part

  // managerHTML function adds office number HTML part

  // internHTML function adds school HTML part

// Function that generates the HTML page
// Export this function